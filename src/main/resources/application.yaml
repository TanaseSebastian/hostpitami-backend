spring:
  application:
    name: hostpitami-backend
  profiles:
    active: dev
  security:
    jwt:
      secret: ${JWT_SECRET}
      expiration-seconds: ${JWT_EXPIRATION_SECONDS:86400}
  stripe:
    secretKey: ${STRIPE_SECRET_KEY}
    webhookSecret: ${STRIPE_WEBHOOK_SECRET}
resend:
  api:
    key: re_htTzteJf_hD4Ude2vRYs7fwby1fgy1RM8